<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.scorekeepdraft1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameActivity"
            android:parentActivityName=".SetTeamsActivity" />

        <provider
            android:name=".data.StatsProvider"
            android:authorities="com.example.android.scorekeepdraft1"
            android:exported="false" />

        <activity android:name=".StandingsActivity" />
        <activity
            android:name=".StatsActivity"
            android:label="PLAYER STATS"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SetLineupActivity"
            android:label="Lineup Editor"
            android:parentActivityName=".SetTeamsActivity" />
        <activity
            android:name=".SetTeamsActivity"
            android:label="Matchup Selector"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".undoredo.PlayerPageActivity" />
        <activity android:name=".PlayerPageActivity"></activity>
    </application>

</manifest>