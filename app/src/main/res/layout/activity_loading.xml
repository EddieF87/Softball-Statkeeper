<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="xyz.sleekstats.softball.activities.LoadingActivity">

    <TextView
        android:id="@+id/load_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:layout_above="@id/load_desc"
        android:textSize="30sp"
        tools:text="Loading" />

    <TextView
        android:id="@+id/load_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_centerInParent="true"
        android:gravity="center"
        tools:text="This is a description of the loading process" />

    <ProgressBar
        android:id="@+id/load_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/load_desc"
        android:layout_marginTop="8dp"
        android:progress="0"
        android:visibility="invisible"/>
</RelativeLayout>
